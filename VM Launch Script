#!/bin/bash

# Variables
RESOURCE_GROUP="MyResourceGroup"
VM_NAME="MyVM"

# Start the VM
az vm start \
  --resource-group $RESOURCE_GROUP \
  --name $VM_NAME

echo "VM $VM_NAME started successfully."
